
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: saham-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data:ro
    restart: unless-stopped
